package lc27

type optimalDivisionElement struct {
	max, min       float64
	maxPos, minPos int
}

func optimalDivision(nums []int) string {
	N := len(nums)
	table := make([]optimalDivisionElement, N*N)
	for L := 1; L < n; L++ {
		for i := 0; i < n-L; i++ {

		}
	}
}
